# Sjósund Nauthólsvík – Cloudflare Worker

Deploy með Cloudflare Workers/Wrangler.

- Build command: `npm install`
- Deploy command: `npx wrangler deploy`

API:
- `/api/tides`
- `/api/nautholsvik`
- `/health`
